
<div class="list-group">
    <a href="javascript:void(0);" class="list-group-item active">
        Tài khoản
    </a>
    <ul class="list-group">
        <li class="list-group-item">
            @Ajax.ActionLink("Upload Avatar", "UploadAvatar", "Account", null,
                        new AjaxOptions
                        {
                            UpdateTargetId = "Update",
                            InsertionMode = InsertionMode.Replace,
                            HttpMethod = "Post"
                        }, null)
        </li>
        <li class="list-group-item">
            @Ajax.ActionLink("Cập nhật", "Edit", "Manage", new { userName = User.Identity.Name},
                            new AjaxOptions
                            {
                                UpdateTargetId = "Update",
                                InsertionMode = InsertionMode.Replace,
                                HttpMethod = "Get"
                            }, null)
        </li>
        <li class="list-group-item">
            @Ajax.ActionLink("Đổi mật khẩu", "Edit", "Manage", new { userName = User.Identity.Name },
                            new AjaxOptions
                            {
                                UpdateTargetId = "Update",
                                InsertionMode = InsertionMode.Replace,
                                HttpMethod = "Get"
                            }, null)
        </li>
    </ul>
</div>