@model Web.Models.Url

<h5>Embed</h5>
<div class="row">
    <div class="col-md-4">
        <img src="@Model.AvatarUrl.GetUrls().FirstOrDefault()" class="img-fluid" alt="Responsive image" />
        @*@Html.Raw(Model.AvatarUrl)*@
    </div>
    <div class="col-md-8">
        @Html.Raw(Model.Title)
        <br />
        @Html.Raw(Model.Address)
    </div>
</div>

